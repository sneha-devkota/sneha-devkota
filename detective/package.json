{
  "name": "detective-animation",
  "version": "1.0.0",
  "description": "Detective GitHub grid animation",
  "main": "index.html",
  "scripts": {
    "build": "echo 'Static HTML file - no build required'",
    "start": "npx http-server . -p 3000"
  }
}
